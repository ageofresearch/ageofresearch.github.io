# Changelog

## Unreleased — planned 1.0.0

- Prepared the first publication candidate.
- Added the complete Lean development for the exact `k = 2` equality, the
  `k ≥ 3` ratio limit, and eventual strict inequality.
- Pinned Lean 4.32.1 and the exact Mathlib commit.
- Added guarded axiom auditing, semantic mapping, reproducibility
  documentation, source citations, and AI disclosure.

After the first tagged release, tags and assets are immutable. Corrections will
use a new version.
